## 通过Swift的Visison框架抠图

```
swiftc removebg.swift -o removebg

REMOVEBG_PNGQUANT=1 ./removebg 1 ./input/pigRelax/ ./output/pigRelax/ --outputFmt png

./removebg 1 ./input/pigStudy/ ./output/pigStudy/ --outputFmt webp --webpQuality 100

# hybrid模式，当前只对绿幕背景图去除背景的效果比较好，一般情况下比vision好；如果非绿幕背景，还是优先使用vision模式
hybrid 绿幕 mask 和 Vision mask 做合并，优先保住非绿色前景区域；如果 Vision 失败，会自动回退成纯绿幕模式。

# 新增去水印参数
# --watermarkRemoval on|off
# --watermarkRepairRadius 0-30
```

## 解决通过ffmpeg将webp序列合并为mp4视频时出现明显锯齿的问题

```
# 如果你的视频最终是要放在浅色背景上播放的，不要让 FFmpeg 默认用黑色填充透明区域，而是主动给视频垫一个与你最终播放环境相近的背景色（比如白色）。
# 注意：请将 s=1920x1080 替换为你原始 WebP 图片的真实分辨率。
# 原理：先将半透明的像素与白色背景进行正确的 Alpha 混合，生成没有透明通道但边缘平滑的帧，然后再转为 yuv420p。这样在浅色背景下播放时，锯齿感会大幅降低。
ffmpeg -framerate 20 -i %03d.webp -vf "color=c=black:s=1080x1080,format=rgba[bg];[0:v]format=rgba[fg];[bg][fg]overlay=format=auto,format=yuv420p" -t 5 output.mp4
```

## A2D文件操作说明

### 将webp序列打包封装到a2d文件
```
python ani2d_tool.py encode \
  --inputImgs "../swift/output/pigSleep/%03d.webp" \
  --stateNames default \
  --fps 15 \
  --sizeMode auto \
  --atlasOptimize auto \
  --aniFile output/pigSleep.a2d \
  --rawFrameFormat webp

python ani2d_tool.py encode \
  --inputImgs "../swift/output/pigWorking/%03d.webp" \
  --stateNames default \
  --fps 15 \
  --sizeMode auto \
  --atlasOptimize auto \
  --aniFile output/pigWork.a2d \
  --rawFrameFormat webp \
  --workers 8
```

## 生成a2d文件的集成脚本说明

```shell
$ bash autogen_ani2d.sh --help
args: [-i , -o , -f] [--input=, --output=, --frameRate=]
# 使用示例
$ bash autogen_ani2d.sh -f=15 -m=hybrid -i=./ani2d/source/pigCurious.mp4 -o=./ani2d/output/pigCurious.a2d
$ bash autogen_ani2d.sh -f=15 -m=hybrid -i=./ani2d/source/pigLooking.mp4 -o=./ani2d/output/pigLooking.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/pigReading.mp4 -o=./ani2d/output/pigReading.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/pigTalking.mp4 -o=./ani2d/output/pigTalking.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/pigTeaching.mp4 -o=./ani2d/output/pigTeaching.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/pigWalking.mp4 -o=./ani2d/output/pigWalking.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/pigWorking.mp4 -o=./ani2d/output/pigWorking.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/eatNoodles.mp4 -o=./ani2d/output/eatNoodles.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/eatFinish.mp4 -o=./ani2d/output/eatFinish.a2d
$ bash autogen_ani2d.sh -f=15 -m=vision -i=./ani2d/source/afterEat.mp4 -o=./ani2d/output/afterEat.a2d
```

## 识别图中物体(不识别具体物体名，只识别物体位置坐标信息)

```shell
swift ExtraFoodList.swift ./input/food1.png
```
